type Candidate={id:string;familyId:string;isActive:boolean;publishedAt:Date|null;gender:string|null;brandId:string|null;basePrice:{toNumber():number};createdAt:Date;variants:Array<{isActive:boolean;stockQuantity:number}>};
export function selectRelated<T extends Candidate>(current:Candidate,candidates:T[],limit=4):T[]{const base=Math.max(1,current.basePrice.toNumber()),score=(item:T)=>(current.gender&&item.gender===current.gender?100:0)+(current.brandId&&item.brandId===current.brandId?80:0)+(item.variants.some(variant=>variant.isActive&&variant.stockQuantity>0)?40:0)+Math.max(0,20-Math.abs(item.basePrice.toNumber()-base)/base*20);return candidates.filter(item=>item.familyId!==current.familyId&&item.isActive&&item.publishedAt!==null).sort((a,b)=>score(b)-score(a)||b.createdAt.getTime()-a.createdAt.getTime()||a.id.localeCompare(b.id)).filter((item,index,all)=>all.findIndex(candidate=>candidate.familyId===item.familyId)===index).slice(0,limit)}
