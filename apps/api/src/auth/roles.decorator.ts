import { SetMetadata } from '@nestjs/common';import type { UserRole } from '@footwear/shared';export const ROLES_KEY='allowed_roles';export const Roles=(...roles:UserRole[])=>SetMetadata(ROLES_KEY,roles);
