import { describe,expect,it } from 'vitest';import{canTransitionOrder}from'./order-lifecycle';describe('order lifecycle',()=>{it('allows the normal COD progression',()=>{expect(canTransitionOrder('PENDING','CONFIRMED')).toBe(true);expect(canTransitionOrder('CONFIRMED','PROCESSING')).toBe(true);expect(canTransitionOrder('PROCESSING','SHIPPED')).toBe(true);expect(canTransitionOrder('SHIPPED','DELIVERED')).toBe(true)});it('allows cancellation only before shipping',()=>{expect(canTransitionOrder('PENDING','CANCELLED')).toBe(true);expect(canTransitionOrder('PROCESSING','CANCELLED')).toBe(true);expect(canTransitionOrder('SHIPPED','CANCELLED')).toBe(false)});it('keeps delivered and cancelled orders terminal',()=>{expect(canTransitionOrder('DELIVERED','PENDING')).toBe(false);expect(canTransitionOrder('CANCELLED','PROCESSING')).toBe(false)})});
