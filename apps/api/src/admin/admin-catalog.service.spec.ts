import{describe,expect,it,vi}from'vitest';import{AdminCatalogService}from'./admin-catalog.service';
const actor={id:'admin',name:'Admin',email:'admin@raqi.dev',role:'SUPER_ADMIN'}as never;
describe('AdminCatalogService brands',()=>{it('deletes an assigned brand without deleting products',async()=>{const tx={brand:{delete:vi.fn().mockResolvedValue({id:'nike'})}};const prisma={brand:{findUnique:vi.fn().mockResolvedValue({id:'nike',name:'Nike'})},product:{deleteMany:vi.fn()},$transaction:vi.fn((callback:(client:typeof tx)=>unknown)=>callback(tx))};const audit={write:vi.fn()};const service=new AdminCatalogService(prisma as never,audit as never);await expect(service.deleteBrand(actor,'nike')).resolves.toEqual({deleted:true,id:'nike'});expect(tx.brand.delete).toHaveBeenCalledWith({where:{id:'nike'}});expect(prisma.product.deleteMany).not.toHaveBeenCalled();expect(audit.write).toHaveBeenCalledWith(actor,'BRAND_DELETED','Brand','nike',{name:'Nike'},tx)})});
