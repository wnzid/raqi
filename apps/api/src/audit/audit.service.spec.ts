import {describe,expect,it,vi}from'vitest';import{AuditService}from'./audit.service';
describe('AuditService retention',()=>{it('deletes rows older than exactly three days at the database level and still writes new events',async()=>{const prisma={auditLog:{deleteMany:vi.fn().mockResolvedValue({count:2}),create:vi.fn().mockResolvedValue({id:'new'})}},service=new AuditService(prisma as never),now=new Date('2026-08-15T15:00:00.000Z');await service.cleanup(now);expect(prisma.auditLog.deleteMany).toHaveBeenCalledWith({where:{createdAt:{lt:new Date('2026-08-12T15:00:00.000Z')}}});await service.write({id:'actor',name:'Admin',email:'admin@raqi.dev',role:'MANAGER'}as never,'PRODUCT_UPDATED','Product','product');expect(prisma.auditLog.create).toHaveBeenCalledTimes(1)})});
