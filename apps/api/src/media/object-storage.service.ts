import { BadRequestException, Injectable } from '@nestjs/common'; import { mkdir,readFile,unlink,writeFile } from 'node:fs/promises'; import path from 'node:path'; import { randomUUID } from 'node:crypto';
@Injectable() export class ObjectStorageService {private readonly root=path.resolve(process.env.MEDIA_LOCAL_PATH??'./uploads'); async put(productId:string,file:{buffer:Buffer;mimetype:string;size:number}){const ext:{[key:string]:string}={'image/jpeg':'jpg','image/png':'png','image/webp':'webp'};if(!ext[file.mimetype]||!file.size||file.size>5*1024*1024)throw new BadRequestException('Only non-empty JPEG, PNG, or WebP images up to 5 MB are allowed');const key=`products/${productId}/${randomUUID()}.${ext[file.mimetype]}`;const target=this.safe(key);await mkdir(path.dirname(target),{recursive:true});await writeFile(target,file.buffer);return key} async remove(key:string){await unlink(this.safe(key)).catch(()=>undefined)} async read(key:string){return readFile(this.safe(key))} private safe(key:string){const target=path.resolve(this.root,key);if(!target.startsWith(`${this.root}${path.sep}`))throw new BadRequestException('Invalid media key');return target}}
