import{BadRequestException}from'@nestjs/common';import{describe,expect,it}from'vitest';import{ObjectStorageService}from'./object-storage.service';describe('ObjectStorageService validation',()=>{it('rejects executable uploads',async()=>{await expect(new ObjectStorageService().put('product-1',{buffer:Buffer.from('x'),mimetype:'application/javascript',size:1})).rejects.toBeInstanceOf(BadRequestException)});it('rejects oversized images before writing',async()=>{await expect(new ObjectStorageService().put('product-1',{buffer:Buffer.from('x'),mimetype:'image/png',size:5242881})).rejects.toBeInstanceOf(BadRequestException)})});
