import React from'react';import{afterEach,describe,expect,it}from'vitest';import{cleanup,fireEvent,render,screen}from'@testing-library/react';import{ProductGallery}from'./product-gallery';import type{ProductDetail}from'@footwear/shared';
afterEach(cleanup);const media=(id:string,position:number,url=`https://example.com/${id}.webp`)=>({id,type:'IMAGE',objectKey:id,url,altText:id,position,mediaPosition:null,isPrimary:position===0,variantId:null})as ProductDetail['media'][number];
describe('ProductGallery',()=>{it('orders images and navigates next and previous',()=>{render(<ProductGallery colorwayId="black" title="Runner" images={[media('third',2),media('first',0),media('second',1)]}/>);expect(screen.getByAltText('first')).toBeTruthy();fireEvent.click(screen.getByLabelText('Next product image'));expect(screen.getByAltText('second')).toBeTruthy();fireEvent.click(screen.getByLabelText('Next product image'));expect(screen.getByAltText('third')).toBeTruthy();fireEvent.click(screen.getByLabelText('Previous product image'));expect(screen.getByAltText('second')).toBeTruthy()});it('resets to the first image when the colorway changes',()=>{const view=render(<ProductGallery colorwayId="black" title="Runner" images={[media('black-one',0),media('black-two',1)]}/>);fireEvent.click(screen.getByLabelText('Next product image'));view.rerender(<ProductGallery colorwayId="blue" title="Runner" images={[media('blue-one',0),media('blue-two',1)]}/>);expect(screen.getByAltText('blue-one')).toBeTruthy()});it('hides arrows for a single image and prevents dragging',()=>{render(<ProductGallery colorwayId="black" title="Runner" images={[media('only',0)]}/>);expect(screen.queryByLabelText('Next product image')).toBeNull();expect(screen.getByAltText('only').getAttribute('draggable')).toBe('false')});it('opens at the active image, navigates, and closes the modal',()=>{render(<ProductGallery colorwayId="black" title="Runner" images={[media('first',0),media('second',1)]}/>);fireEvent.click(screen.getAllByLabelText('Next product image')[0]!);fireEvent.click(screen.getByLabelText('Enlarge product image'));expect(screen.getByRole('dialog')).toBeTruthy();expect(screen.getAllByAltText('second')).toHaveLength(2);fireEvent.click(screen.getAllByLabelText('Next product image')[1]!);expect(screen.getAllByAltText('first')).toHaveLength(2);fireEvent.click(screen.getByLabelText('Close image viewer'));expect(screen.queryByRole('dialog')).toBeNull()})});
