'use client';
/* eslint-disable @next/next/no-img-element */
import React from'react';import Link from'next/link';import type{ProductDetail}from'@footwear/shared';
export function ColorwayOptions({product}:{product:ProductDetail}){return <fieldset className="mt-6"><legend className="text-sm font-semibold">Color: <span className="font-normal muted">{product.color.name}</span></legend><div className="mt-3 flex flex-wrap gap-3">{product.siblings.map(sibling=><Link aria-current={sibling.id===product.id?'page':undefined} className={`w-20 border p-1 text-center text-xs ${sibling.id===product.id?'border-black':'border-[var(--line)]'}`} href={`/product/${sibling.slug}`} key={sibling.id}>{sibling.primaryImageUrl?<span className="block aspect-[4/3] bg-[var(--soft)]"><img className="h-full w-full select-none object-contain" src={sibling.primaryImageUrl} alt="" draggable={false} onContextMenu={event=>event.preventDefault()} onDragStart={event=>event.preventDefault()}/></span>:<span className="grid aspect-[4/3] place-items-center bg-[var(--soft)] text-[.6rem] font-bold tracking-widest text-neutral-400">RAQI</span>}<span className="mt-1 block truncate">{sibling.color.name}</span></Link>)}</div></fieldset>}
