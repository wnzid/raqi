import { publicEnvironment } from './env';
export type ManagedUser={id:string;name:string;email:string;role:'CUSTOMER'|'MANAGER'|'SUPER_ADMIN';isActive:boolean;createdAt:string;updatedAt:string};
export type AuditEntry={id:string;actor:{name:string;email:string};actorRole:string;action:string;entityType:string;entityId:string|null;metadata:Record<string,unknown>|null;createdAt:string};
async function request<T>(path:string,init?:RequestInit){const response=await fetch(`${publicEnvironment.NEXT_PUBLIC_API_URL}/super-admin${path}`,{...init,credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json',...init?.headers}});if(!response.ok)throw new Error(await response.text());return response.json() as Promise<T>}
export const superAdminApi={users:(init?:RequestInit)=>request<{data:ManagedUser[];pagination:{total:number}}>('/users',init),logs:(init?:RequestInit)=>request<{data:AuditEntry[];pagination:{total:number}}>('/audit-logs',init),create:(body:unknown)=>request<ManagedUser>('/users',{method:'POST',body:JSON.stringify(body)}),role:(id:string,role:string)=>request<ManagedUser>(`/users/${id}/role`,{method:'PATCH',body:JSON.stringify({role})}),status:(id:string,isActive:boolean)=>request<ManagedUser>(`/users/${id}/status`,{method:'PATCH',body:JSON.stringify({isActive})})};
