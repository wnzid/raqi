import { publicEnvironment } from './env';export async function createAdminVariant(productId:string,input:unknown){const response=await fetch(`${publicEnvironment.NEXT_PUBLIC_API_URL}/products/${productId}/variants`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(input)});if(!response.ok)throw new Error(`Variant creation failed (${response.status})`);return response.json()}
