import type {ProductListResponse} from '@footwear/shared';
import {describe,expect,it} from 'vitest';
import {selectLandingProducts,selectNewArrivals,visibleColorways} from './home-products';
type Product=ProductListResponse['data'][number];
function product(id:string,familyId:string,overrides:Partial<Product>={}):Product{return{id,title:`Colorway ${id}`,slug:`shoe-${id}`,family:{id:familyId,name:`Model ${familyId}`,slug:`model-${familyId}`},color:{id,name:id,slug:id,hex:'#000000'},description:'',basePrice:3500,effectivePriceFrom:3500,primaryImageUrl:`https://example.com/${id}.jpg`,brand:null,gender:'MEN',material:null,soleType:null,heelType:null,isActive:true,isNewArrival:false,isAvailable:true,createdAt:'2026-08-15T00:00:00.000Z',...overrides}}
describe('homepage product selection',()=>{it('uses only active admin-selected colorways independently',()=>{const grey=product('grey','runner',{isNewArrival:true}),white=product('white','runner',{isNewArrival:true}),black=product('black','runner'),hidden=product('hidden','runner',{isNewArrival:true,isActive:false});expect(selectNewArrivals([grey,white,black,hidden]).map(item=>item.id)).toEqual(['grey','white'])});it('keeps active colorways from the same family as separate cards',()=>{const black=product('black','kayano'),blue=product('blue','kayano'),hidden=product('hidden','other',{isActive:false});expect(visibleColorways([black,blue,hidden])).toEqual([black,blue])});it('prefers available products and keeps each colorway slug',()=>{const unavailable=product('grey','runner',{isAvailable:false}),available=product('black','runner');const result=selectLandingProducts([unavailable,available]);expect(result.map(item=>item.slug)).toEqual(['shoe-black','shoe-grey'])})});
