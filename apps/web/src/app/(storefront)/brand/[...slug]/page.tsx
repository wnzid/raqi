import { CatalogView } from '@/components/catalog/catalog-view';
import { listProducts } from '@/lib/api-client';
export default async function BrandPage({params,searchParams}:{params:Promise<{slug:string[]}>;searchParams:Promise<Record<string,string|string[]|undefined>>}) { const {slug}=await params; const raw=await searchParams; const query=Object.fromEntries(Object.entries(raw).filter((entry):entry is [string,string]=>typeof entry[1]==='string')); const brand=slug.at(-1)??''; const catalogParams={...query,brand}; const result=await listProducts(catalogParams); const title=brand.split('-').map(word=>word.charAt(0).toUpperCase()+word.slice(1)).join(' '); return <CatalogView result={result} title={title} params={catalogParams} breadcrumbs={[{label:'Home',href:'/'},{label:'Footwear',href:'/products'},{label:title}]}/> }
