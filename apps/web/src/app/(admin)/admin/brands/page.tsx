import { headers } from 'next/headers';import { TaxonomyManager } from '@/components/admin/taxonomy-manager';import { adminApi } from '@/lib/admin-api';export default async function Page(){const h=await headers();const items=await adminApi.brands({headers:{cookie:h.get('cookie')??''}});return <section><p className="eyebrow">Catalog</p><h1 className="title mt-3">Brands</h1><TaxonomyManager kind="brands" initial={items}/></section>}
