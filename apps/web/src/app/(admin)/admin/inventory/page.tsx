import { headers } from 'next/headers';import { InventoryTable } from '@/components/admin/inventory-table';import { adminApi } from '@/lib/admin-api';export default async function Page(){const h=await headers();const [products]=await adminApi.products({headers:{cookie:h.get('cookie')??''}});return <section><p className="eyebrow">Catalog operations</p><h1 className="title mt-3">Inventory</h1><p className="mt-3 muted">Update current stock for each sellable SKU. Values must be whole numbers at or above zero.</p><InventoryTable products={products}/></section>}
